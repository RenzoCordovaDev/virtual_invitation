# src/layout/

Shell de la app: el contenedor de scroll-snap de pantalla completa que
renderiza, en orden, las secciones de los módulos activos en
`src/config/features.ts` (ver `docs/DESIGN.md` sección 2), y el layout de
dashboard del panel admin (`/admin/*`, sin scroll-snap).
